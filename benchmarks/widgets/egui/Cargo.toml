[package]
name = "egui-widget-bench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
egui = "=0.32.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
codegen-units = 1
lto = "thin"
