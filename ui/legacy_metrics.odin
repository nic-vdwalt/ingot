package ui

sync_legacy_metrics :: proc(metrics: Ui_Metrics) {
	FONT_SIZE = metrics.FONT_SIZE
	FONT_SIZE_LARGE = metrics.FONT_SIZE_LARGE
	FONT_SIZE_SMALL = metrics.FONT_SIZE_SMALL
	LINE_HEIGHT = metrics.LINE_HEIGHT
	FONT_SIZE_TITLE = metrics.FONT_SIZE_TITLE
	FONT_SIZE_BODY = metrics.FONT_SIZE_BODY
	FONT_SIZE_LABEL = metrics.FONT_SIZE_LABEL
	FONT_SIZE_NOTE = metrics.FONT_SIZE_NOTE
	NVIM_FONT_SIZE = metrics.NVIM_FONT_SIZE
	NVIM_CELL_PAD = metrics.NVIM_CELL_PAD
	NVIM_MARGIN = metrics.NVIM_MARGIN
	TAB_BAR_HEIGHT = metrics.TAB_BAR_HEIGHT
	CAPTION_BTN_W = metrics.CAPTION_BTN_W
	INPUT_BAR_HEIGHT = metrics.INPUT_BAR_HEIGHT
	PADDING = metrics.PADDING
	TAB_WIDTH = metrics.TAB_WIDTH
	TAB_MIN_WIDTH = metrics.TAB_MIN_WIDTH
	TAB_CLOSE_SIZE = metrics.TAB_CLOSE_SIZE
	TAB_ICON_SIZE = metrics.TAB_ICON_SIZE
	COMMAND_ITEM_HEIGHT = metrics.COMMAND_ITEM_HEIGHT
	POPUP_MAX_WIDTH = metrics.POPUP_MAX_WIDTH
	SCROLL_SPEED = metrics.SCROLL_SPEED
	CHAT_MAX_W = metrics.CHAT_MAX_W
	MSG_GAP = metrics.MSG_GAP
	USER_CARD_PAD_H = metrics.USER_CARD_PAD_H
	USER_CARD_PAD_V = metrics.USER_CARD_PAD_V
	USER_CARD_RADIUS_PX = metrics.USER_CARD_RADIUS_PX
	USER_CARD_MIN_W = metrics.USER_CARD_MIN_W
	ROW_H_SM = metrics.ROW_H_SM
	ROW_H_MD = metrics.ROW_H_MD
	PANEL_HEADER_H = metrics.PANEL_HEADER_H
	CARD_RADIUS_PX = metrics.CARD_RADIUS_PX
	CONTROL_BOX = metrics.CONTROL_BOX
	CONTROL_GAP = metrics.CONTROL_GAP
	SLIDER_TRACK_H = metrics.SLIDER_TRACK_H
	SLIDER_KNOB_R = metrics.SLIDER_KNOB_R
	MENU_ITEM_H = metrics.MENU_ITEM_H
	MENU_PAD = metrics.MENU_PAD
	MENU_MIN_W = metrics.MENU_MIN_W
	TOOLTIP_PAD = metrics.TOOLTIP_PAD
	ATTACHMENT_CHIP_ROW_H = metrics.ATTACHMENT_CHIP_ROW_H
	DROP_ZONE_H = metrics.DROP_ZONE_H
	TOOL_BORDER_W = metrics.TOOL_BORDER_W
	TOOL_CARD_PAD_V = metrics.TOOL_CARD_PAD_V
	TOOL_CARD_PAD_H = metrics.TOOL_CARD_PAD_H
	TOOL_CARD_GAP = metrics.TOOL_CARD_GAP
	CODE_BLOCK_PAD = metrics.CODE_BLOCK_PAD
	BULLET_INDENT = metrics.BULLET_INDENT
	TABLE_CELL_PAD = metrics.TABLE_CELL_PAD
	PLAN_SIDEBAR_W = metrics.PLAN_SIDEBAR_W
	PLAN_SIDEBAR_COLLAPSED_W = metrics.PLAN_SIDEBAR_COLLAPSED_W
	PLAN_SIDEBAR_ROW_H = metrics.PLAN_SIDEBAR_ROW_H
	PLAN_TITLE_ACCENT_W = metrics.PLAN_TITLE_ACCENT_W
	PLAN_TITLE_PAD = metrics.PLAN_TITLE_PAD
	DEBUG_SIDEBAR_W = metrics.DEBUG_SIDEBAR_W
	DEBUG_SIDEBAR_COLLAPSED_W = metrics.DEBUG_SIDEBAR_COLLAPSED_W
	DEBUG_SIDEBAR_ROW_H = metrics.DEBUG_SIDEBAR_ROW_H
	DEBUG_TITLE_ACCENT_W = metrics.DEBUG_TITLE_ACCENT_W
	DEBUG_TITLE_PAD = metrics.DEBUG_TITLE_PAD
	SHELLS_PANEL_W = metrics.SHELLS_PANEL_W
	SPLIT_DIVIDER_W = metrics.SPLIT_DIVIDER_W
	VORTEX_RADIUS = metrics.VORTEX_RADIUS
	VORTEX_INNER = metrics.VORTEX_INNER
	WAVE_BAR_W = metrics.WAVE_BAR_W
	WAVE_BAR_GAP = metrics.WAVE_BAR_GAP
	WAVE_BAR_MAX_H = metrics.WAVE_BAR_MAX_H
	WAVE_BAR_MIN_H = metrics.WAVE_BAR_MIN_H
}
