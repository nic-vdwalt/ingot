# Grounded whole-blade LOD verification — 2026-09-12

## Verified implementation

The offline cooker protects grounded discrete roots and checks clustered level grounding without
changing cluster simplification. Opt-in `grass_blades_2` preserves complete double-sided blades,
including different opposite triangulation diagonals, using authoritative FACE IDs. Blender export
preserves those IDs through evaluation, triangulation and saved-scene reload. INGMESH2 is unchanged.

All four PlanetForger varieties were tagged, refined from the original scene and recooked. Repeating
refinement from the original scene and checking regenerated bytes produces the same bundle:

`4aed6387a2b8dcca5efd04d90dd36eee227fab1888dea8dc6b56569c136949f6`

| Grass | Source blades | Coarse blades | Fine triangles | Coarse triangles | Decoded minimum Z |
|---|---:|---:|---:|---:|---:|
| Upright | 16 | 4 | 192 | 48 | 0 at both levels |
| Crossed | 16 | 4 | 192 | 48 | 0 at both levels |
| Reed | 12 | 3 | 144 | 36 | 0 at both levels |
| Tuft | 18 | 4 | 216 | 48 | 0 at both levels |

Ground constraints also changed generic LOD counts: conifers now have four levels each and rocks
have two each. Actual totals are 12 meshes, 30 levels, 20,165 vertices and 22,356 indices. Shared
decoder capacities match these measured totals and also accommodate TerraForger's existing payload.
Largest individual levels remain within 4,542 vertices and 4,992 indices.

## Evidence

Evidence is in the sibling `ingot-artifacts` repository, not the Ingot source tree:

- `grass-preview.py`: reproducible Blender source-geometry preview and cook audit.
- `grass-audit.json`: per-ID retained blade IDs, fine/coarse bounds, triangles and conservative errors.
- `grass-lods-front.png`, `grass-lods-reverse.png`: inspected fine/coarse side-by-side previews.
- `grass-preview.log`: rendering and audit output.
- `grass-tests.log`: successful Ingot native test gate, including WSS loopback matrix.
- `grass-check-final.log`: successful Ingot strict/check/format gate.
- `grass-planet-tests.log`: PlanetForger 290 shared, 567 client and 8 host tests passed.
- `grass-planet-check-final.log`: successful final PlanetForger check.
- `grass-terra-check.log`: successful TerraForger check.
- `grass-terra-tests-final.log`: unresolved TerraForger regression failures described below.

Dependency-free Python tests passed: 57 tests, with eight Blender-only tests skipped under ordinary
Python. All eight Blender integration tests passed under Blender 5.2.0. Tests include actual saved
scene export and freshness checks, metadata corruption, wrong attribute domains, rooted packed and
unpacked geometry, complete blade preservation, crossing/nonmanifold rejection and policy limits.
Three distinct Python hash seeds produced identical synthetic cooked bytes. A 256-blade fixture
successfully reduced 1,024 triangles to 256 in approximately 0.067 seconds on this machine; this is
an offline sanity measurement, not a calibrated rendering benchmark.

Front and reverse previews show rooted complete fine/coarse blades and distinct upright, spreading,
reed and tuft shapes. Coarse silhouettes are intentionally thinner. Blender Workbench preview is
not native game backface-culling or wind acceptance; both sides are validated geometrically by
oriented coverage and exact source-triangle preservation. Native grassland density, wind and frame
performance remain in the separately paused grass presentation plan.

## Remaining compatibility blocker

TerraForger's full test suite was run twice. Its 91 shared tests pass; five of 258 client tests fail:

- `flora_bundle_carries_cooked_lod_chains`
- `flora_embedded_bundle_contains_every_mesh`
- `flora_mesh_recipes_derive_grounded_valid_meshes`
- `flora_scatter_static_and_ground_instances_spawn_at_full_scale`
- `structure_node_variants_are_deterministic_and_complete`

The embedded sibling bundle has eight mesh IDs while the shared flora suite expects twelve;
structure variants report three components where that suite expects two. This patch does not
change TerraForger's bundle, asset IDs, scatter, structure recipes or those tests. The shared decoder
capacity increase does not change its expected mesh count. Fixing or aligning that sibling's
pre-existing asset/test contract requires separate authorization; do not copy PlanetForger assets
over it or weaken shared tests to claim compatibility.

Concurrent unrelated repository changes caused transient check failures during the run. No such
source was reverted or fixed by this task. Final Ingot and PlanetForger checks passed; the repeated
TerraForger client failures remain unresolved. Plan step 5 therefore remains incomplete pending
sibling compatibility resolution. Steps 2–6 of the original grass presentation plan remain paused.

## Resume verification — 2026-09-13

Reran `python3 -m unittest discover ingot/tools`: 57 Python tests passed and eight Blender-only
 tests were skipped. Reran TerraForger `bash build.sh check && bash build.sh test`: check passed,
all 91 shared tests passed, and four of 258 client tests failed. The three flora bundle/recipe
failures still report missing meshes (12 expected, eight present), and the full-scale scatter test
still reports no biological ground cover. The previously failing structure variant test now passes
in the current worktree; this task did not modify its implementation or tests.

The compatibility gate still exits with status 1. Completed implementation and earlier preview,
Blender integration and determinism evidence are preserved; those latter checks were not rerun
in this resume attempt. Step 5 remains blocked on the sibling flora asset/scatter contracts.
Resolution requires separate authorization to change those contracts or an externally supplied fix;
resuming this prerequisite plan alone does not authorize that scope expansion.

## Authorized compatibility corrections — 2026-09-13

The user subsequently authorized correcting the sibling failures. Shared bundle tests now validate
all shipped contiguous IDs within the supported 8–12 range rather than requiring the maximum
storage capacity. Recipe derivation tests use the same extracted `_flora_asset_resolve` helper
as production upload, preserving existing tree, tuft and shrub fallback geometry and scales.
The scatter test checks full-scale grass in both demos, requiring lineage only when the demo
actually enables ecology. TerraForger deliberately disables ecology; zero lineage is expected.
No authored assets or scatter production behavior were changed.

PlanetForger check and all four focused flora tests pass after these corrections. A direct full
client run passes all 574 tests. Its aggregate suite still has an unrelated shared failure in
`clear_resource_nodes_preserves_players_and_linked_buildings` and timed out after 600 seconds;
the client compatibility affected here is fully verified.

The user authorized continuing, so TerraForger's stale ForgeCore pin was aligned from
`bf1f62d45b378efe70c0b7351dd5efb192e1c7fe` to the same tested ForgeCore revision PlanetForger
already uses, `53e18e2ddf3220b79a28fd8af94df1ee14ba3d28`. TerraForger check passes, followed by all
91 shared, 261 client and eight host tests. Ingot's pinned-toolchain check and complete native test
gate pass. Together with the previously verified previews, Blender tests, deterministic recooking
and Python cooker tests, repository compatibility for this prerequisite repair is complete.
